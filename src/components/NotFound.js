import { NotFoundContainer } from "./StyledComponents"

function NotFound() {
    return(
        <NotFoundContainer>Sorry, that page could not be found.</NotFoundContainer>
    )
}

export default NotFound